'use strict';

var dom = require('./libs/dom.js');

function app() {
    var elApp = dom.findId('app'),
        config;

    elApp.innerHTML = '<div id="" class=""></div>';

    config = {};
}

module.exports = app;
